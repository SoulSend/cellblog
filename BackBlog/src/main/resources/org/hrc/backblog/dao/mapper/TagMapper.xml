<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hrc.backblog.dao.mapper.TagMapper">

    <select id="findTagsByArticleId" parameterType="long" resultType="org.hrc.backblog.dao.pojo.Tag">
        select id,avatar,tag_name tagName from ms_tag
            where id in
                (select tag_id from ms_article_tag where article_id=#{articleId})
    </select>
    <select id="findHostsTagByTagIds" parameterType="list" resultType="org.hrc.backblog.dao.pojo.Tag">
        select id,tag_name tagName from ms_tag
            where id in
                <foreach collection="tagIds" item="tagId" separator="," open="(" close=")">
                    #{tagId}
                </foreach>
    </select>
    <select id="findHotsTagIds" parameterType="int"  resultType="java.lang.Long">
        select tag_id from ms_article_tag at group by tag_id order by count(1) desc limit #{size}
    </select>
</mapper>